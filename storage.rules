rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Anyone can read images and resume files
    match /{allPaths=**} {
      allow read: if true;
      // Only authenticated admin can upload, modify or delete files
      allow write: if request.auth != null;
    }
  }
}
